name: GithubActoinsTest
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello-World
        env:
          PRINT: Hello-World3
          run: cd scratch-gui/&&npm start
        run: |
          echo $PRINT
