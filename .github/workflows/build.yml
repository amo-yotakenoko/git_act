name: GithubActoinsTest
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello-World
        env:
          PRINT: Hello-World2
        run: |
          echo $PRINT
