name: Start
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: run multi command
        run: |
          git clone https://github.com/amo-yotakenoko/scratch-gui.git
          ls
          cd scratch-gui
          ls
          npm run build
